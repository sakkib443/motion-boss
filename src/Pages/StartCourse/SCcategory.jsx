import React from 'react';

const SCcategory = ({item}) => {
    return (
        <div>
        <button className='btn w-full mb-4'>   {item.classname}</button>
        
        </div>
    );
};

export default SCcategory;